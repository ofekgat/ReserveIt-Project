# Local development defaults only. Override in production via environment
# variables: DB_URL, DB_USER, DB_PASSWORD, DB_DRIVER, DB_POOL_INITIAL_SIZE, DB_POOL_MAX_SIZE.
# Never commit real production credentials to this file.
db.url=jdbc:mysql://localhost:3306/getticket?useSSL=false&serverTimezone=UTC
db.user=root
db.password=changeme
db.driver=com.mysql.cj.jdbc.Driver
db.pool.initialSize=5
db.pool.maxSize=15
